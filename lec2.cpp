#include <bits/stdc++.h>
using namespace std;

int main()
{
    // smallest addressable memory used in bool datatypes is 1 byte not 1 bit
    // float takes 4 byte memory whereas double takes 8 byte so double[64] shows more precsion than float[32]
    // 1's compliment is to flip the binary numbers and ...
    // 2s compliment is (1s compliment + 1)..
    // signed cam store +/- pr zero but unsigned only store positive value
    // int - 4 byte - 32 bits - 2^32bits memory
    //(2^n)-1  = where occupies all memory location with 1 .
    //for signed integer [-2^31 to (2^31)-1]
    //outer loop runs for the rows from top to bottom 
    //inner loop runs for the column form left to right
    
    return 0;
}